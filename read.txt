создаем Ѕƒ в MySQL ->create database Hotel;
перейдем дл€ работы с этой Ѕƒ ->use Hotel;
сменит текущую кодировку на UTF-8  ->set names utf8;
если у кого-то с первого раза не получитьс€ создать таблицу или оа будет крива€ убейте ее ->drop table <name>;
посмотрим структуру созданной таблицы ->explain client;
посмотреть скрипт создани€ таблицы  ->show create table client;
делаем то же самое с остальными таблицами

